import React from 'react'
import ProjectCard from '../project-card/project-cart'

const ProjectsContainer = () => {
  return (
    <section className='w-full h-[80vh] relative flex'>
      <ProjectCard />
    </section>
  )
}

export default ProjectsContainer
